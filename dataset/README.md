# Dataset Description

This folder includes two datasets — training and benchmark — used for training and evaluating the task offloading algorithm between edge and IoT devices.

## Columns

- time slot – Sequential time intervals (e.g., 1, 2, 3, …).

- edge_cpu – CPU usage of the edge device at that time slot.

- iot_cpu – CPU usage of the IoT device at that time slot.
